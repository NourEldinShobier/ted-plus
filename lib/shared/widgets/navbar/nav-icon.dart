import 'package:flutter/material.dart';

class NavIcon {
  Icon icon;
  Icon activeIcon;

  String title;
  Color color;

  NavIcon({
    this.icon,
    this.activeIcon,
    this.title,
    this.color,
  });
}
