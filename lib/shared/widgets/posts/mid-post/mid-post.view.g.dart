// GENERATED CODE - DO NOT MODIFY BY HAND

part of 'mid-post.view.dart';

// **************************************************************************
// FunctionalWidgetGenerator
// **************************************************************************

class MidPost extends StatelessWidget {
  const MidPost({Key key, @required this.talk, @required this.pageOffset})
      : super(key: key);

  final Talk talk;

  final double pageOffset;

  @override
  Widget build(BuildContext _context) =>
      midPost(talk: talk, pageOffset: pageOffset);
  @override
  int get hashCode => hashValues(talk, pageOffset);
  @override
  bool operator ==(Object o) =>
      identical(o, this) ||
      (o is MidPost &&
          identical(talk, o.talk) &&
          identical(pageOffset, o.pageOffset));
}

class MidPostBody extends StatelessWidget {
  const MidPostBody({Key key}) : super(key: key);

  @override
  Widget build(BuildContext _context) => midPostBody(_context);
}

class _TalkImage extends StatelessWidget {
  const _TalkImage({Key key}) : super(key: key);

  @override
  Widget build(BuildContext _context) => _talkImage(_context);
}

class _TalkDuration extends StatelessWidget {
  const _TalkDuration({Key key}) : super(key: key);

  @override
  Widget build(BuildContext _context) => _talkDuration(_context);
}

class _TalkThumbnail extends StatelessWidget {
  const _TalkThumbnail({Key key}) : super(key: key);

  @override
  Widget build(BuildContext _context) => _talkThumbnail(_context);
}

class _TalkTitle extends StatelessWidget {
  const _TalkTitle({Key key}) : super(key: key);

  @override
  Widget build(BuildContext _context) => _talkTitle(_context);
}

class _TalkSubTitle extends StatelessWidget {
  const _TalkSubTitle({Key key}) : super(key: key);

  @override
  Widget build(BuildContext _context) => _talkSubTitle(_context);
}

class _Views extends StatelessWidget {
  const _Views({Key key}) : super(key: key);

  @override
  Widget build(BuildContext _context) => _views(_context);
}

class _BookMark extends StatelessWidget {
  const _BookMark({Key key}) : super(key: key);

  @override
  Widget build(BuildContext _context) => _bookMark(_context);
}

class _More extends StatelessWidget {
  const _More({Key key}) : super(key: key);

  @override
  Widget build(BuildContext _context) => _more(_context);
}
